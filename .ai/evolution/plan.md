# Evolution Plan

Propose-only plan derived from deterministic findings. AMO does not apply
these changes itself; a human or supervised agent must.

## Low priority

1. Add tests or map existing tests to their subjects. (`missing_test_edges`, graph)
2. Add fixture ground truth (truth.json) and rerun `amo benchmark`. (`benchmark_unscored_precision_recall`, benchmark)
