# Validators

AMO validators protect memory from drift and pollution.

Initial validators:

- broken references
- stale context packs
- runtime pollution
- artifact hygiene
- basic drift
- unsafe instructions

Validation writes `.ai/machine/validation.json`.
